 let healthDebounce ={}socket.element.addEventListener("players",event =>{let data = event.data if(global.currentMap == "none")return;let localTeam = "" for(let player of data.players){if(player.isLocal){localTeam = player.team global.localPlayerNum = player.num break}}for(let player of data.players){let playerDot = global.playerDots[player.num] let playerLabel = global.playerLabels[player.num] let classes = [player.team] if(typeof player.color === "number" && player.color >= 0 && player.color <= 5){classes.push("color" + player.color)}if(localTeam && player.team && player.team !== localTeam){classes.push("enemy")}if(player.health <= 0){healthDebounce[player.num] =(healthDebounce[player.num] || 0)+ 1 if(healthDebounce[player.num] >= 2){classes.push("dead")global.playerBuffers[player.num] = [] global.playerPos[player.num].alive = false}}else{healthDebounce[player.num] = 0 global.playerPos[player.num].alive = true if(player.bomb)classes.push("bomb")if(player.active)classes.push("active")if(global.config.radar.flashes && player.flashed > 0.5)classes.push("flashed")if(global.config.radar.shooting){let prevAmmo = global.playerAmmos[player.num] ||{}let currAmmo = player.ammo let currWeapon = Object.keys(currAmmo)[0] let prevWeapon = Object.keys(prevAmmo)[0] if(currWeapon && currWeapon === prevWeapon){let diff = prevAmmo[currWeapon] - currAmmo[currWeapon] if(diff > 0 && diff <= 5){classes.push("shooting")}}if(currWeapon){global.playerAmmos[player.num] = currAmmo}}if(global.config.radar.damage){if(player.health < global.playerHealths[player.num]){classes.push("hurting")}global.playerHealths[player.num] = player.health}let effectivePos = player.position if(player.m_velocity &&(player.m_velocity[0] || player.m_velocity[1] || player.m_velocity[2])){let now = performance.now()global.playerLastUpdate = global.playerLastUpdate ||{}let last = global.playerLastUpdate[player.num] if(last){let dt =(now - last)/ 1000 if(dt < 0.05)dt = 0.05 if(dt > 0.2)dt = 0.2 effectivePos ={x: player.position.x + player.m_velocity[0] * dt,y: player.position.y + player.m_velocity[1] * dt,z: player.position.z + player.m_velocity[2] * dt}}global.playerLastUpdate[player.num] = now}global.playerPos[player.num].x = global.positionToPerc(effectivePos,"x",player.num)global.playerPos[player.num].y = global.positionToPerc(effectivePos,"y",player.num)global.playerPos[player.num].a = player.angle global.playerPos[player.num].z = effectivePos.z}let newClasses = classes.join(" ")if(playerDot.className != "dot " + newClasses)playerDot.className = "dot " + newClasses if(playerLabel.className != "label " + newClasses)playerLabel.className = "label " + newClasses global.playerPos[player.num].alive = player.health > 0 let infoText = "" let useHtml = false if(global.config.radar.showName == "both" || global.config.radar.showName == "always"){infoText = player.name.substring(0,global.config.radar.maxNameLength)}if(player.health > 0){let weaponMode = global.config.radar.showWeapon if(weaponMode === "name"){let weaponName = player.weapons.m_active || "" let abbrev = weaponName.replace("weapon_","").replace("_silencer","").replace("ak47","AK").replace("m4a1","M4").replace("awp","AWP").replace("usp","USP").replace("glock","Glock").replace("deagle","Deagle").replace("mp5sd","MP5").replace("mp7","MP7").replace("mp9","MP9").replace("mac10","MAC").replace("ump45","UMP").replace("p90","P90").replace("bizon","Bizon").replace("nova","Nova").replace("xm1014","XM").replace("sawedoff","Saw").replace("mag7","Mag").replace("m249","M249").replace("negev","Negev").replace("ssg08","SSG").replace("sg556","SG").replace("aug","AUG").replace("famas","Famas").replace("galilar","Galil").replace("scar20","Scar").replace("g3sg1","G3").replace("knife","Knife").replace("bayonet","Knife").replace("c4","C4").toUpperCase()if(infoText)infoText += " " infoText += abbrev}else if(weaponMode === "icon"){let weaponName = player.weapons.m_active || "" if(weaponName){let iconKey = weaponName.replace("weapon_","").replace("bayonet","knife")let iconHtml = '<img class="weapon-icon" src="/img/icons/' + iconKey + '.svg" alt="' + iconKey + '" onerror="this.style.display=\'none\'">' if(infoText)infoText += " " infoText += iconHtml useHtml = true}}if(global.config.radar.showHealth){if(infoText)infoText += " " infoText += player.health + "HP"}}if(useHtml){playerLabel.children[0].innerHTML = infoText}else{playerLabel.children[0].textContent = infoText}if(global.bomb.state === "planted" && global.bomb.countdown < 100 && global.config.radar.showBlastRadius === 'active' && global.mapData.survivableDistance && player.health > 0){const dx = player.position.x - global.bomb.position.x const dy = player.position.y - global.bomb.position.y const distance = Math.sqrt(dx * dx + dy * dy);const healthIndex = Math.min(global.mapData.survivableDistance.length - 1,Math.floor(player.health / 5))const survivableDistance = global.mapData.survivableDistance[healthIndex] if(distance < survivableDistance){const norm = Math.sqrt(dx * dx + dy * dy)const dirX = dx / norm const dirY = dy / norm const safeX = global.bomb.position.x + dirX * survivableDistance const safeY = global.bomb.position.y + dirY * survivableDistance const arrowSize = 14 * global.mapData.resolution const arrowOffset = arrowSize * -0.25 global.playerBlasts[player.num][0].setAttribute("stroke",(survivableDistance - distance >(global.bomb.countdown - 1)* 250)? "rgb(195,20,20,.9)" : "")global.playerBlasts[player.num][0].setAttribute("d",` M ${global.positionToPerc(player.position,"x")}${100 - global.positionToPerc(player.position,"y")}L ${global.positionToPerc(safeX,"x")}${100 - global.positionToPerc(safeY,"y")}M ${global.positionToPerc(safeX + dirX * arrowOffset + dirY * arrowSize,"x")}${100 - global.positionToPerc(safeY + dirY * arrowOffset - dirX * arrowSize,"y")}A ${arrowSize / 20},${arrowSize / 20}0 0 0 ${global.positionToPerc(safeX + dirX * arrowOffset - dirY * arrowSize,"x")},${100 - global.positionToPerc(safeY + dirY * arrowOffset + dirX * arrowSize,"y")}`)}else{global.playerBlasts[player.num][0].setAttribute("stroke","")global.playerBlasts[player.num][0].setAttribute("d","")}}else if(global.playerBlasts && global.playerBlasts[player.num]){global.playerBlasts[player.num][0].setAttribute("stroke","")global.playerBlasts[player.num][0].setAttribute("d","")}}})socket.element.addEventListener("roundend",event =>{global.followedPlayer = null for(let j = 0;j < 10;j++){if(global.playerDots[j]){global.playerDots[j].classList.remove("followed")}}for(let num in global.playerBuffers){global.playerBuffers[num] = [] global.playerPos[num] ={x: null,y: null,z: null,a: null,alive: false}global.playerDots[num].style.display = "none" global.playerDots[num].style.display = "block" global.playerLabels[num].style.display = "none" global.playerLabels[num].style.display = ""}})for(let i = 0;i < 10;i++){let dot = global.playerDots[i] if(!dot)continue dot.style.cursor = "pointer" dot.addEventListener("click",function(e){e.stopPropagation()if(global.followedPlayer === i){global.followedPlayer = null}else{global.followedPlayer = i}for(let j = 0;j < 10;j++){if(global.playerDots[j]){global.playerDots[j].classList.remove("followed")}}if(global.followedPlayer !== null && global.playerDots[global.followedPlayer]){global.playerDots[global.followedPlayer].classList.add("followed")}})}document.getElementById("container").addEventListener("click",function(){if(global.followedPlayer !== null){global.followedPlayer = null for(let j = 0;j < 10;j++){if(global.playerDots[j]){global.playerDots[j].classList.remove("followed")}}}})