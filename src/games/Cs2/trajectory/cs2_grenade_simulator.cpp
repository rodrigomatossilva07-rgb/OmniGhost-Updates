#include "cs2_grenade_simulator.h"
#include <algorithm>
#include <cmath>
namespace CS2::Trajectory { namespace { Vec3 Add(Vec3 a,Vec3 b){return{a.x+b.x,a.y+b.y,a.z+b.z};} Vec3 Sub(Vec3 a,Vec3 b){return{a.x-b.x,a.y-b.y,a.z-b.z};} Vec3 Mul(Vec3 a,float s){return{a.x*s,a.y*s,a.z*s};} float Dot(Vec3 a,Vec3 b){return a.x*b.x+a.y*b.y+a.z*b.z;} float Length(Vec3 v){return std::sqrt(Dot(v,v));} void Record(TrajectoryResult& r,Vec3 p){if(r.points.empty()||r.points.size()<256)r.points.push_back(p);} }
TrajectoryResult SimulateGrenade(const CollisionBvh& world,Vec3 pos,Vec3 vel,const GrenadePhysics& p){TrajectoryResult out{};constexpr float base=1.f/64.f;Record(out,pos);unsigned bounces=0;for(unsigned tick=0;tick<2048&&out.elapsed<p.detonate_seconds;++tick){const unsigned substeps=std::clamp(static_cast<unsigned>(std::ceil(Length(vel)*base/(std::max)(p.max_segment_length,4.f))),1u,12u);const float dt=base/substeps;for(unsigned sub=0;sub<substeps&&out.elapsed<p.detonate_seconds;++sub){vel.z-=p.gravity*dt;const Vec3 next=Add(pos,Mul(vel,dt));const auto hit=world.TraceRay(pos,next);if(!hit.hit)pos=next;else{pos=hit.position;out.hit=true;if(out.bounces.size()<32)out.bounces.push_back(pos);const float normal=Dot(vel,hit.normal);if(normal<0)vel=Sub(vel,Mul(hit.normal,(1.f+p.restitution)*normal));const Vec3 normalPart=Mul(hit.normal,Dot(vel,hit.normal));vel=Add(normalPart,Mul(Sub(vel,normalPart),p.surface_friction));if(Length(vel)<28.f||++bounces>=p.max_bounces){out.settled=true;out.impact=pos;return out;}}out.elapsed+=dt;}if((tick&1u)==0)Record(out,pos);}out.impact=pos;return out;}
TrajectoryResult SimulateBallistic(Vec3 pos,Vec3 vel,const GrenadePhysics& p){TrajectoryResult out{};constexpr float dt=1.f/64.f;Record(out,pos);for(unsigned tick=0;tick<2048&&out.elapsed<p.detonate_seconds;++tick){vel.z-=p.gravity*dt;pos=Add(pos,Mul(vel,dt));out.elapsed+=dt;if((tick&1u)==0)Record(out,pos);}out.impact=pos;return out;}
}
