#include "security.h"

// Implementations for AntiDebug / AntiDump / IntegrityVerifier live as
// inline definitions in security.h. This translation unit intentionally
// avoids re-defining those symbols.
